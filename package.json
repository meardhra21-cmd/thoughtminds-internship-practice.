{
  "scripts": {
    "build": "npm install --prefix Ardhra-Profile && npm run build --prefix Ardhra-Profile && mkdir -p dist && cp -r Ardhra-Profile/dist/. dist && cp -r Ardhra-Profile/dist/. ."
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.1",
    "lucide-react": "^1.21.0",
    "tailwindcss": "^4.3.1"
  }
}
